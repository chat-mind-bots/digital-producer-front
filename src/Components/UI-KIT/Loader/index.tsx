import * as ST from './styled';

const Loader = () => (
  <ST.Loader>
    <ST.WrapperAnimation>
      <ST.Outter />
      <ST.Inner />
    </ST.WrapperAnimation>
  </ST.Loader>
);

export default Loader;
